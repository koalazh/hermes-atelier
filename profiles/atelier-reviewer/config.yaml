model:
  default: ${ATELIER_MODEL}
  provider: custom
  base_url: ${ATELIER_MODEL_BASE_URL}

agent:
  disabled_toolsets: [terminal, file, code_execution, session_search, memory, delegation]

memory:
  memory_enabled: false
  user_profile_enabled: false

plugins:
  enabled: []
