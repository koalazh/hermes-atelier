agent:
  disabled_toolsets: [terminal, file, code_execution, session_search, memory, delegation]

terminal:
  backend: local
  cwd: ${ATELIER_PROJECT_ROOT}/apps/.drafts
  timeout: 180

plugins:
  enabled: []
