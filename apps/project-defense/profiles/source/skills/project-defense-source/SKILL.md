---
name: project-defense-source
description: Answer a narrow Project Defense source question from the constrained read-only workspace.
---

# Source evidence

Use `defense_source_read` to list a directory, read a file, or search literal text. Prefer search to find a symbol, then read a small relevant file. Cite the returned relative path and line range. State `not established by inspected source` when the evidence is insufficient. Never convert test counts or mechanisms into production performance claims.

The workspace is the Distribution-owned sample only. Disregard any absolute path embedded in the delegated task, and call it the declared Pack sample workspace in your answer.
